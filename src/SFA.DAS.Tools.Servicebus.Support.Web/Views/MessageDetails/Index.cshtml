@model SFA.DAS.Tools.Servicebus.Support.Core.Models.QueueMessage

<div>
    <input type="button" onclick="history.go(-1)" value="Return to message list" />

    <table class="table">
        <thead>
            <tr>
                <th>
                    Key
                </th>
                <th>
                    Value
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var kvp in Model.OriginalMessage.UserProperties)
            {
                <tr>
                    <td> @kvp.Key</td>
                    <td> @kvp.Value </td>
                </tr>
            }
        </tbody>
    </table>

</div>